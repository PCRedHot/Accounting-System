#include "account.h"

account* getAccount(string, account*);
account* getAccount(float, account*);
void switchAccount(account*, account*);
void sortAccount_Balance(account*);
void rsortAccount_Balance(account*);
